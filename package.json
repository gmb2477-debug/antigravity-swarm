config = yaml.safe_load(f)
import json
    # --- DEBUG START ---
    print("\n[DEBUG] CONTENIDO EXACTO DEL YAML:")
    print(json.dumps(config, indent=2))
    print("[DEBUG] FIN DEL CONTENIDO\n")
    # --- DEBUG END ---
